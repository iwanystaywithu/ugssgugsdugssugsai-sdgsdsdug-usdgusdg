services:
  - type: web
    name: telegram-giftsniper
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 1 -k gthread --threads 4 -b 0.0.0.0:$PORT app:app"
